server:
  port: 9200
spring:
  application:
    name: serivce-product
  h2:
    console:
      enabled: true
  datasource:
    url: dbc:h2:mem:productBD;
    driver-class-name: org.h2.Driver
    platform: h2
    username: sa
    password:
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect