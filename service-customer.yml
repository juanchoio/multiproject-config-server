server:
  port: 9100
spring:
  application:
    name: service-customer
  h2:
    console:
      enabled: true
  datasource:
    url: dbc:h2:mem:customerBD;
    driver-class-name: org.h2.Driver
    platform: h2
    username: sa
    password:
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect